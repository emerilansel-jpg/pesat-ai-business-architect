# Version History

## v5.13.1 — 2026-07-20
- **Type:** INFRA / FIX
- **Changes:**
  - Fixed 404 on `https://pesat.ai/advisor/` by restoring the Cloudflare DNS A record to the VPS origin.
  - Removed the Cloudflare Worker custom domain from `pesat.ai` so requests reach the VPS Caddy server.
  - Rebuilt and redeployed the latest `dist/` to the VPS build volume.
  - Added canonical redirect: `https://apps.pesat.ai/advisor/*` → `https://pesat.ai/advisor/`.
  - Added `VERSIONS.md` and the in-app `/version` page.
- **Files touched:**
  - `VERSIONS.md`
  - `src/pages/Version.tsx`
  - `src/data/versions.ts`
  - `src/App.tsx`
  - `src/components/Navbar.tsx`
  - `src/pages/Admin.tsx`
  - `package.json`
  - `scripts/generate-versions.js`
- **Breaking:** no

## v5.13.0 — 2026-07-21
- **Type:** UI/UX Polish / Prompt Engineering
- **Changes:**
  - Fixed duplicate "Lainnya" buttons: now any choice starting with "Lainnya" or containing "ketik sendiri" is filtered out.
  - AI is now strictly instructed to use "aku/kamu" or "saya/anda" and never "gue/lo".
  - Mobile activity panel bottom sheet reduced from 70vh to 55vh and now exposes a CSS variable so the chat area pads itself, keeping chat bubbles visible.
  - Strengthened image placement prompts to require images at top, middle, and bottom of sections like an article.
  - Stronger visual section separation in AI messages: more vertical spacing and border-top dividers before headings.
  - WhatsApp CTA now renders as a clickable green button using `[CTA:https://wa.me/...]` tag.
  - Improved API error message to suggest checking provider/API key and updating the server `.env`.
- **Files touched:**
  - `src/components/ChatMessage.tsx`
  - `src/components/ActivityPanel.tsx`
  - `src/components/ChatArea.tsx`
  - `src/services/mainPrompt.ts`
  - `src/services/settings.ts`
  - `src/services/visualization.ts`
  - `src/App.tsx`
  - `package.json`
  - `VERSION.md`
  - `coldstart.md`
- **Breaking:** no


## v5.12.0 — 2026-07-20
- **Type:** UI/UX Polish / Prompt Engineering
- **Changes:**
  - Activity Panel status messages are now funnier and more varied: expanded template fallback set and stronger LLM prompt with game-like team narrative.
  - Activity Panel motion effects upgraded: added mascot particle trail, stage-change floating badges, diagonal sheen on progress bar, and pulsing background glow.
  - Images now instructed to appear at the top, middle, or bottom of sections like a well-designed article; added explicit rules to avoid clustering all images at the end.
  - AI "P" avatar now blinks; user avatar has a subtle star-blink sparkle.
  - Mobile chat requests now use a shorter timeout (30s) with one automatic retry on timeout, plus clearer error messages that suggest switching to DeepSeek or checking the API key/OpenAI quota.
  - Increased row spacing and padding between content sections in AI message bubbles.
  - AI is now strictly forbidden from assuming answers when the user clicks "Saya jawab semuanya sekarang" or "Saya jawab satu per satu" without providing actual values.
- **Files touched:**
  - `src/services/activityMessages.ts`
  - `src/components/ActivityPanel.tsx`
  - `src/services/visualization.ts`
  - `src/services/mainPrompt.ts`
  - `src/components/AIAvatar.tsx`
  - `src/components/ChatMessage.tsx`
  - `src/App.tsx`
  - `package.json`
  - `VERSION.md`
  - `coldstart.md`
- **Breaking:** no


## v5.11.0 — 2026-07-20
- **Type:** UI/UX Polish
- **Changes:**
  - Fixed mobile multiple-choice button text visibility with high-contrast white buttons and dark text (`break-words`, stronger border, larger touch target).
  - Replaced AI avatar flash icon with a brand "P" badge across the welcome screen and chat messages.
  - Activity Panel now has more game-like motion effects: floating sparkles around the mascot, a radial pulse on stage changes, bouncy spring log items, and a pulsing glow on the progress bar.
  - Activity Panel status messages are now generated by the LLM per request, falling back to the existing template generator if the LLM call fails or returns empty.
  - User avatar continues to show the detected brand name initials (already implemented in v5.10.0).
- **Files touched:**
  - `src/components/ChatMessage.tsx`
  - `src/components/AIAvatar.tsx`
  - `src/components/WelcomeScreen.tsx`
  - `src/components/ActivityPanel.tsx`
  - `src/App.tsx`
  - `src/services/activityMessages.ts`
  - `package.json`
  - `VERSION.md`
  - `coldstart.md`
- **Breaking:** no


## v5.10.0 — 2026-07-20
- Type: UI/UX Polish
- Changes:
  - Fixed multiple-choice button text visibility on desktop dark mode by using a dark purple gradient with white text.
  - Replaced AI avatar text with a flash (Zap) icon.
  - User avatar now shows the brand initials after the AI detects the brand name (via `[BRAND:Name]` marker).
  - Activity Panel now has more motion effects: log items slide in, progress bar has a shimmer effect, character and status text re-animate on stage changes, and confetti bursts on success.
  - Activity Panel status messages are now varied and task-aware using a template generator (AI generator helper included, ready to activate once OpenAI quota is restored).
- Files touched:
  - `src/components/ChatMessage.tsx`
  - `src/components/AIAvatar.tsx`
  - `src/components/ActivityPanel.tsx`
  - `src/App.tsx`
  - `src/contexts/BrandContext.tsx` (new)
  - `src/services/activityMessages.ts` (new)
  - `src/main.tsx`
  - `package.json`
  - `VERSION.md`
- Breaking: no


## v5.9.0 — 2026-07-20
- Type: Infrastructure / Deploy
- Changes:
  - Migrated production URL from `https://apps.pesat.ai/advisor/` to `https://pesat.ai/advisor/`.
  - Removed `pesat.ai` worker custom domain from `pesat-ai-homepage` so the apex domain resolves to the VPS.
  - Added A record `pesat.ai` → `148.230.103.98` in Cloudflare DNS.
  - Updated Caddy config to serve the advisor app under the apex domain `pesat.ai` on ports 80 and 443.
  - Verified desktop and mobile views load correctly at the new URL.
- Files touched:
  - `scripts/advisor-proxy.js` (deployed)
  - `/opt/pesat-control-plane/caddy/Caddyfile` on VPS
  - `package.json`
  - `VERSION.md`
- Breaking: no (old `apps.pesat.ai/advisor/` URL still works because DNS and Caddy for `apps.pesat.ai` are unchanged).


## v5.8.0 — 2026-07-19
- Type: Feature / Bug Fix
- Changes:
  - Activity Panel no longer appears on desktop when idle; it now only shows during real processing or when logs are present.
  - Mobile Activity Panel supports a true drag gesture on the handle to collapse the sheet, matching the Kimi-style bottom sheet behavior.
  - Replaced the Minion-style character with an original Indonesian robot mascot "CendekiaBot" (graduation cap, batik motif, Pesat badge).
  - Assistant text and images are now released together: images are generated before the message is shown, with a fun expert-team narrative in the Activity Panel.
  - First user message now asks for the primary business challenge with the exact choices requested (omset, hemat, brand, fraud, BI, forecast, lainnya).
  - Strengthened mobile multiple-choice button contrast so text is clearly readable.
  - Bumped promptVersion to 4 and re-enabled autoImageGen by default so existing users pick up the new behavior.
- Files touched:
  - `src/components/ActivityPanel.tsx`
  - `src/contexts/ActivityContext.tsx`
  - `src/App.tsx`
  - `src/components/ChatMessage.tsx`
  - `src/services/settings.ts`
  - `src/services/visualization.ts`
  - `package.json`
  - `VERSION.md`
- Breaking: no


## v5.7.0 — 2026-07-17
- Type: Feature / Bug Fix
- Changes:
  - Redesigned mobile ActivityPanel into a Kimi agent-style bottom sheet that slides up to 70% of the screen.
  - Added a drag handle at the top of the mobile sheet; users can drag it down to collapse.
  - When collapsed, a persistent mini horizontal bar floats above the input bar and reopens the sheet on tap.
  - Fixed ActivityPanel status bug where the message could still read like an active processing step after the AI finished; fun message now follows the current icon/stage correctly.
  - Strengthened language default: Bahasa Indonesia is the default; English is used only when the user clearly writes in English.
  - Added a focused first-message instruction so the opening response follows the main prompt and asks for business details naturally.
  - Bumped prompt version to auto-reset cached step prompts in localStorage.
- Files touched:
  - `src/components/ActivityPanel.tsx`
  - `src/contexts/ActivityContext.tsx`
  - `src/App.tsx`
  - `src/services/settings.ts`
  - `src/services/mainPrompt.ts`
  - `package.json`
  - `VERSION.md`
- Breaking: no


## v5.6.2 — 2026-07-17
- Type: Bug Fix / UI Polish
- Changes:
  - Refined mobile chat to feel closer to Kimi agents app: softer avatar glow, cleaner navbar, rounder message bubbles, and lighter user bubble shadow.
  - Mobile choice buttons now stack full-width instead of wrapping into a grid, matching native mobile chat patterns.
  - Improved touch targets for Copy/Retry actions and made the input bar more pill-shaped on mobile.
  - Replaced the distorted scaled-minion ActivityPanel mobile indicator with a clean spinner/icon badge.
  - Reduced mobile chat spacing and padding for a denser, more app-like feel.
- Files touched:
  - `src/components/WelcomeScreen.tsx`
  - `src/components/Navbar.tsx`
  - `src/components/ChatMessage.tsx`
  - `src/components/InputBar.tsx`
  - `src/components/ActivityPanel.tsx`
  - `src/components/ChatArea.tsx`
  - `src/components/TypingIndicator.tsx`
  - `package.json`
  - `VERSION.md`
- Breaking: no


## v5.6.1 — 2026-07-16
- Type: Bug Fix / UI Polish
- Changes:
  - Redesigned mobile view to look and feel like a native mobile chat app (Kimi-style).
  - Mobile now uses a light theme: white background, light gray AI bubbles, and a floating rounded input bar.
  - Replaced the large mobile ActivityPanel bottom sheet with a compact floating status card above the input.
  - Kept the dark desktop experience unchanged.
  - Added missing CSS utilities for AI avatar glow, online pulse, and typing dot animations.
  - Fixed AI message text color on mobile so it remains readable against the light bubble.
- Files touched:
  - `src/index.css`
  - `src/App.tsx`
  - `src/components/Navbar.tsx`
  - `src/components/ChatArea.tsx`
  - `src/components/ChatMessage.tsx`
  - `src/components/InputBar.tsx`
  - `src/components/TypingIndicator.tsx`
  - `src/components/ScrollToBottom.tsx`
  - `src/components/WelcomeScreen.tsx`
  - `src/components/ActivityPanel.tsx`
  - `src/components/AIAvatar.tsx`
  - `package.json`
  - `VERSION.md`
- Breaking: no


## v5.6.0 — 2026-07-15
- Type: Feature / Bug Fix / Deployment Cleanup
- Changes:
  - ActivityPanel: stage-based game-mode animations, “Quest Complete” badge, and dynamic progress bar.
  - Fixed stuck “crafting response” status with AbortController timeout and guaranteed log completion.
  - First user message now uses MAIN_SYSTEM_PROMPT + Step 1 focus for stronger persona alignment.
  - Moved API keys from source code to `.env` and updated proxy to read from environment variables.
  - Fixed deployment health checks to use proxy port 3002.
  - Made Caddy `/api/*` routing robust with `handle_path` and dynamic Docker host IP detection.
  - Refreshed `COLDSTART.md` to reflect current BrowserRouter, Caddy, and Cloudflare tunnel architecture.
- Files touched:
  - `src/contexts/ActivityContext.tsx`
  - `src/components/ActivityPanel.tsx`
  - `src/App.tsx`
  - `src/services/settings.ts`
  - `src/services/ai.ts`
  - `scripts/advisor-proxy.js`
  - `scripts/deploy.js`
  - `scripts/restart-proxy.js`
  - `scripts/final-deploy.js`
  - `COLDSTART.md`
  - `.env` / `.env.example`
  - `.gitignore`
- Breaking: no

## v5.5.0 — 2026-07-14
- Type: Bug Fix / Feature
- Changes:
  - Redesigned Activity Panel to be fun and game-like: animated minion character that walks, blinks, and celebrates on completion.
  - Added entertaining status messages, progress bar, and random fun facts after completion.
  - Fixed Activity Panel status bug where it could still show "crafting response" after the AI finished.
  - Fixed first-message behavior: now uses the focused Step 1 prompt so the AI asks for business details and follows the main-prompt flow from the start.
  - Strengthened production proxy body-size handling and logging to avoid large-request failures.
- Files touched:
  - `src/contexts/ActivityContext.tsx`
  - `src/components/ActivityPanel.tsx`
  - `src/App.tsx`
  - `src/services/settings.ts`
  - `server/index.ts`
  - `scripts/advisor-proxy.js`
  - `package.json`
  - `VERSION.md`
- Breaking: no

## v5.4.0 — 2026-07-14
- Type: Feature
- Changes:
  - Added real-time Activity Panel during AI processing with typewriter-effect status updates.
  - Desktop: right-side floating panel that collapses to a pill.
  - Mobile: bottom sheet with swipe-down-to-close and floating reopen handle.
  - Integrated activity logging into the chat flow: Thinking → Searching web → Analyzing → Crafting response → Generating visual.
  - Switched image generation to manual by default: users click "Generate visual" instead of waiting for auto-generation.
  - Auto-generate toggle remains available in admin for users who prefer the old behavior.
- Files touched:
  - `src/contexts/ActivityContext.tsx` (new)
  - `src/components/ActivityPanel.tsx` (new)
  - `src/App.tsx`
  - `src/components/ChatMessage.tsx`
  - `src/services/settings.ts`
- Breaking: no
